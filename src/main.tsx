import React from 'react'
import ReactDOM from 'react-dom/client'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div style={{ padding: '2rem', textAlign: 'center', color: '#fff', background: '#0a0a0b', minHeight: '100vh' }}>
      <h1>KORVN Portfolio</h1>
      <p>See DOCUMENTATION.md for the complete HTML/CSS/JS guide.</p>
    </div>
  </React.StrictMode>,
)
